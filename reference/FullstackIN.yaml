openapi: 3.1.0
info:
  title: FullstackIN
  version: '1.0'
  description: This is specification for an API that should serve data for the front-end chart
  contact:
    name: IN
servers:
  - url: 'http://localhost:3000'
paths:
  /companies:
    parameters: []
    get:
      summary: Get list of all companies
      tags: []
      responses:
        '200':
          description: List of companies
          content:
            application/json:
              schema:
                type: array
                minItems: 1
                items:
                  $ref: '#/components/schemas/Company'
              examples:
                Example:
                  value:
                    - id: 1
                      name: Google
                      agent: Adam Smith
                      gender: male
                      meetingDate: '2021-10-31'
                    - id: 2
                      name: Netflix
                      agent: Simen Newton
                      gender: feale
                      meetingDate: '2021-04-11'
        '500':
          description: Something went wrong
      operationId: get-users-userId
      description: Retrieve array of companies.
  /individuals:
    get:
      summary: ''
      operationId: get-user
      responses:
        '200':
          description: List of individuals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Individual'
        '500':
          description: Something went wrong
      description: Retrieve array of individuals
    parameters: []
components:
  schemas:
    Company:
      title: Company
      type: object
      description: Model of company entry
      examples:
        - id: 142
          firstName: Alice
          lastName: Smith
          email: alice.smith@gmail.com
          dateOfBirth: '1997-10-31'
          emailVerified: true
          signUpDate: '2019-08-24'
      properties:
        id:
          type: integer
          description: Unique identifier for the given company.
        name:
          type: string
          description: Name of the company
        agent:
          type: string
          description: Full name of company's agent
        gender:
          type: string
          enum:
            - male
            - female
        meetingDate:
          type: string
          format: date
      required:
        - id
        - name
        - agent
        - gender
        - meetingDate
      x-examples:
        example-1:
          id: 1
          name: Google
          agent: Adam Smith
          gender: male
          meetingDate: '2020-10-31'
    Individual:
      title: Individual
      type: object
      properties:
        id:
          type: integer
          description: Unique id of a person
        name:
          type: string
          description: First name of a person
        surname:
          type: string
          description: Person's surname
        gender:
          type: string
          enum:
            - male
            - female
        meetingDate:
          type: string
          format: date
      required:
        - id
        - name
        - surname
        - gender
        - meetingDate
      description: Model of Individual entry
